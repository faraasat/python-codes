after doing all things we run command "python setup.py sdist bdist_wheel"
to create a wheel

after creating wheel file we will install the 'wh' file in dist folder with
"pip install"

then we can import package int python